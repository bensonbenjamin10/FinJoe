# FinJoe – Agent Reference

Context for AI agents working on this codebase. Read this when you need to query the database, understand schema, or run scripts.

---

## Database Access

### Connection

- **Source**: `DATABASE_URL` in `.env` (root)
- **Provider**: Railway PostgreSQL (public proxy: `*.proxy.rlwy.net`)
- **Schema**: Drizzle ORM in `shared/schema.ts`

### Railway Connection Quirks

Railway URLs (internal `*.railway.internal` or public `*.proxy.rlwy.net`) must use `sslmode=disable` because TLS is handled at the edge. The script and server handle this automatically.

**Do not add `sslmode=require`** to Railway URLs. Use the raw URL; the code adds `?sslmode=disable` when needed.

### Querying the Database

Use the `db:query` script. It loads `.env`, normalizes the connection string, and runs SQL:

```bash
npm run db:query -- "SELECT * FROM expense_categories"
npm run db:query -- "SELECT * FROM income_categories"
npm run db:query -- "SELECT id, name FROM tenants"
```

**Requires**: `DATABASE_URL` in `.env`. If missing, the script exits with a clear error.

---

## Key Tables

### `expense_categories`

| Column | Type | Notes |
|-------|------|-------|
| id | varchar | UUID |
| tenant_id | varchar | null = global template |
| name | text | Display name |
| slug | text | URL-safe identifier |
| parent_id | varchar | Optional parent |
| display_order | integer | Sort order |
| cashflow_label | text | MIS/Cashflow label |
| is_active | boolean | Default true |

### `income_categories`

| Column | Type | Notes |
|-------|------|-------|
| id | varchar | UUID |
| tenant_id | varchar | Required |
| name | text | Display name |
| slug | text | URL-safe identifier |
| income_type | text | Default "other" |
| display_order | integer | Sort order |
| is_active | boolean | Default true |

### Other Core Tables

- `tenants` – Organizations
- `cost_centers` (alias `campuses`) – Branches, departments
- `expenses` – Expense records
- `income_records` – Income records
- `users` – Auth users
- `billing_customers` – Tenant-scoped customer directory for invoicing
- `invoices` – AR-lite sales documents (draft → issued → partially_paid → paid → void)
- `invoice_lines` – Line items per invoice (description, qty, unit_amount, tax_rate, line_total)
- `payment_allocations` – Links payments to invoices; `invoices.amount_paid` derives from sum
- `payment_orders` – Razorpay checkout orders linking verify → income_records

---

## Scripts

| Command | Purpose |
|---------|---------|
| `npm run db:query -- "SQL"` | Run arbitrary SQL (see above) |
| `npm run db:generate` | Generate SQL migration from schema changes |
| `npm run db:migrate` | Apply pending migrations (requires DATABASE_URL) |
| `npm run db:repair-invite-columns` | Idempotent `ALTER` for `users.invite_token_*` if prod missed migration `0007` (see below) |
| `npm run db:studio` | Open Drizzle Studio (visual DB browser) |
| `npm run db:pull` | Introspect live DB into snapshot |
| `npm run dev` | Start API server |
| `npm run worker:dev` | Start worker |
| `npm run backfill:embeddings` | Backfill expense embeddings (needs GEMINI_API_KEY) |

---

## Paths

- **Schema**: `shared/schema.ts`
- **API**: `server/index.ts`, `server/db.ts`
- **Worker**: `worker/src/index.ts`
- **Migrations**: `drizzle/` (generated by drizzle-kit; legacy files in `migrations-legacy/`)
- **Migration config**: `drizzle.config.ts`
- **DB query script**: `scripts/db-query.mjs`
- **Admin FinJoe hub**: `client/src/pages/admin-finjoe-hub.tsx` (path-based sub-routes, not `?tab=`)
- **FinJoe path helpers**: `client/src/lib/finjoe-routes.ts`

### Admin UI — FinJoe & landing (milestones)

| Behavior | Detail |
|----------|--------|
| After login / setup / signup (success) | Redirect to **`/admin/dashboard`** (`login.tsx`, `setup.tsx`, `signup.tsx`) |
| FinJoe area | Single wouter route matching **`FINJOE_AREA_PATH_PATTERN`** → `AdminFinJoeHub` (avoids remount on sub-nav) |
| Canonical paths | `/admin/finjoe/structure/cost-centers`, `/people/contacts`, `/people/users`, `/people/role-requests`, `/integrations/settings` |
| Bare `/admin/finjoe` | **`FinJoeRootRedirect`**: admins → cost centers; other tenant staff → role requests (`FinJoeRedirect.tsx`) |
| Legacy `/admin/team` | Redirect → **`/admin/finjoe/people/users`** (preserves `tenantId`) |
| Sidebar | **FinJoe** + **Team** (tenant admins) deep-link to dashboard users path |
| Docs | `docs/plans/admin-ia-finjoe-team.plan.md`, `docs/plans/admin-ia-implementation.plan.md`, `docs/admin-ia-strategy.md` |

---

## MCP / External Tools

- **Railway MCP**: No database query tools. Use for deployment, logs, variables.
- **Neon MCP**: For Neon databases only; FinJoe uses Railway Postgres.
- **Supabase MCP**: Not used for FinJoe DB.

To query the FinJoe database, use `npm run db:query` with `DATABASE_URL` in `.env`.

---

## Troubleshooting: `users` / `invite_token_hash` query errors

There is **no data backfill** for invites: `invite_token_hash` and `invite_token_expires_at` are nullable. The app fails when those **columns are missing** on the database your API actually uses.

**Common causes**

1. **`db:migrate` used a different `DATABASE_URL` than production** (local `.env` vs Railway).
2. **A migration before `0007` failed**, so Drizzle never reached `0007_user_invite_tokens.sql`.
3. **Deploy never runs migrations** — run `npm run db:migrate` (or your platform’s migrate step) with the **same** `DATABASE_URL` as the running service.

**Verify (against the DB the app uses)**

```bash
npm run db:query -- "SELECT column_name FROM information_schema.columns WHERE table_schema = 'public' AND table_name = 'users' AND column_name LIKE 'invite%'"
```

**Fix without re-ordering Drizzle** (idempotent DDL only)

```bash
npm run db:repair-invite-columns
```

Set `DATABASE_URL` to the **production** URL first (e.g. `railway run` or paste from Railway dashboard). Then redeploy or restart is not required for column add.
